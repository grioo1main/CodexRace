spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/pvp_platform
    username: pvp_user
    password: pvp_pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
